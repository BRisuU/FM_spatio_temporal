

# Attributes
# ----------
# processed_data: pd.DataFrame
# processed_geo_data: gpd.GeoDataFrame

# Functions
# ----------
# reshape_vbz_data(data, geo_data)
# reshape_road_data(data, geo_data)
# reshape_activemode_data(data, geo_data)
# merge_geolocalized_data(data, geo_data)
# inter_dataset_allignment(data1, data2)
# clean_data(data)
# standardize_data(data)
# save_data(data, path)